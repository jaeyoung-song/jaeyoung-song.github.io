// This file contains the JavaScript code for the personal homepage.
// You can add functionality such as event listeners, animations, or dynamic content updates here.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Personal homepage loaded successfully!');

    // Example: Add a click event listener to a button
    const button = document.getElementById('myButton');
    if (button) {
        button.addEventListener('click', () => {
            alert('Button clicked!');
        });
    }

    // Additional JavaScript functionality can be added here
});